# Documentation

Documentation for this repository is maintained under `docs/`.

This `doc/` directory exists to keep the top-level repository layout consistent (`src/`, `test/`, `doc/`) across Verilog design repositories.
